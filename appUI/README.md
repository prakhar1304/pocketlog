# Pocketlog — UI captures & demo

This folder holds **submission media** for the README:

| File | Purpose |
|------|---------|
| `ui1.jpg` … `ui5.jpg` | App screenshots (referenced from the root `README.md`) |
| `demoVideo.mp4` | Short walkthrough demo |

Replace or update these files when refreshing screenshots or the demo video. Paths stay stable so the root README does not need edits.
